import styled from "styled-components";

const ResetButton = () => {
  return (
    <ResetButtonStyle>
      <h1>ResetButton</h1>
    </ResetButtonStyle>
  );
};

const ResetButtonStyle = styled.div``;

export default ResetButton;
